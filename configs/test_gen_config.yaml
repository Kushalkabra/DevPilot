base_model: "your-oumi-base-model"
reward_model: "your-oumi-reward-model"
tokenizer: "your-oumi-tokenizer"
output_dir: "outputs/oumi-test-gen"
max_examples: 200
training:
  # RL-specific parameters to tune for real runs.
  learning_rate: 1.0e-5
  batch_size: 4
  epochs: 1
  kl_coefficient: 0.1
  max_steps: 200
  log_every: 10
  save_every: 50

